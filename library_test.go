package main

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("API Test", func() {
	Describe("Given a developer has a valid API key and location", func() {
		Context("when a developer requests a Fahrenheit temperature", func() {
			It("should receive a valid temperature in Fahrenheit", func() {

			})
		})
		Context("when a developer requests a temperature string", func() {
			It("should receive a valid temperature string", func() {

			})
		})
	})
})
